// Minimal deterministic revenue leak detector placeholder.
// For now returns an empty list; callers can extend taxonomy later.

const detectRevenueLeaks = (_scoredDeals = []) => {
	return [];
};

module.exports = { detectRevenueLeaks };
